const header = document.querySelector(".navbar")

/**
window.onscroll = function() { // when the user scrolls far enough, we want the nav bar to be dark
    var top = window.scrollY; // y pos of scroll/mouse
    if(top >=100) {
        header.classList.add('navbarDark');
    }
    else {
        header.classList.remove('navbarDark');
    }
}
*/